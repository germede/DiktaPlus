<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBarOverlay">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- AlertDialog style-->
    <style name="MyTitleTextStyle">
        <item name="android:textColor">@color/primary_text_color</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowTitleStyle">@style/MyTitleTextStyle</item>    <!-- Used for the title -->
        <item name="colorAccent">@drawable/alert_dialog_state_text</item>       <!-- Used for the buttons -->
        <item name="android:textColorPrimary">@color/primary_text_color</item>  <!-- Used for the title and text -->
        <item name="android:textColor">@color/primary_text_color</item>         <!-- Used for the title and text -->
        <item name="android:textStyle">bold</item>                              <!-- Used for style of the buttons -->
        <item name="android:background">@color/window_background</item>         <!-- Used for the background -->
    </style>

    <!-- Base input -->

    <!-- Text header primary color -->

    <!-- Text primary color -->

    <!-- Button primary -->
    <style name="ButtonPrimaryColor">
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
        <item name="android:background">@color/color_primary</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_marginTop">@dimen/button_padding</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">15dp</item>
    </style>

    <!-- Button secondary -->
    <style name="ButtonSecondaryColor">
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
        <item name="android:background">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_marginTop">@dimen/button_padding</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingLeft">15dp</item>
        <item name="android:paddingRight">15dp</item>
    </style>

    <!-- Button left -->
    <style name="ButtonLeft">
        <item name="android:background">@color/transparent</item>
        <item name="android:src">@drawable/ic_arrow_left</item>
        <item name="android:layout_height">25dp</item>
        <item name="android:layout_width">25dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <!-- Button right -->
    <style name="ButtonRight">
        <item name="android:background">@color/transparent</item>
        <item name="android:src">@drawable/ic_arrow_right</item>
        <item name="android:layout_height">25dp</item>
        <item name="android:layout_width">25dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <!-- Texts switcher label -->

</resources>
